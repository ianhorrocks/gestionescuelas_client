version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npx doppler run --command="npm install"
    build:
      commands:
        - npx doppler run --command="npm run build"
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
