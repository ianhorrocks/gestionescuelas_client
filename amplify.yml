version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npx doppler run -- npm install
    build:
      commands:
        - npx doppler run -- npm run build
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
