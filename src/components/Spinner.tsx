import React from "react";
import styles from "../styles/GeneralComponents/_Spinner.module.scss";

const Spinner: React.FC = () => {
  return <div className={styles.spinner}></div>;
};

export default Spinner;